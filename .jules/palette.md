## 2025-06-06 - Contextual ARIA labels on repeated script editors
**Learning:** Generic "Expand" or "Reset" labels on identical icon-only buttons across multiple script editors create confusion for screen reader users, as the context is lost.
**Action:** Always include specific descriptive text (e.g., incorporating the section title) in the `aria-label` when identical controls appear multiple times on the same page.
