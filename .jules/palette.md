## 2024-03-25 - Initial UX Findings
**Learning:** Found an opportunity to improve accessibility on `Approvals.tsx` by providing an explicit `id` and `aria-label` or `htmlFor` association for the feedback input field in `ScriptApprovalCard`.
**Action:** Enhance form inputs with proper labels and ARIA attributes for screen reader compatibility.
