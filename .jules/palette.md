# Palette's Journal

**2025-05-15 - Starting UX Enhancement**
**Learning:** The project uses `pnpm` and has a `lint` script that was broken due to missing config.
**Action:** Always check `pnpm lint` and fix configuration if necessary before starting work to ensure a clean baseline (or at least a known baseline).
